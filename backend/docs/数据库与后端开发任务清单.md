# 数据库与后端开发任务清单

更新时间：2026-07-30

## 状态说明

- `[ ]` 未开始
- `[-]` 进行中
- `[x]` 已完成并通过验收
- `[!]` 阻塞，等待确认或外部条件

## 总体计划表

| 编号 | 阶段 | 主要工作 | 完成标准 | 状态 |
|---|---|---|---|---|
| 0 | 文档一致性 | 同步需求、数据库和接口文档，冻结已确认业务规则 | 三份递增版本文档生成并通过结构校验 | `[x]` |
| 1 | 技术基线 | 确认框架、ORM、迁移工具、Redis、测试框架和目录规范 | 技术选型及版本写入项目说明 | `[x]` |
| 2 | 工程初始化 | 创建应用入口、配置、日志、异常处理、健康检查和依赖清单 | 服务可启动，配置可按环境加载 | `[x]` |
| 3 | 新数据库建表 | 按数据库设计 V1.3 建立全部业务表、索引、外键、约束及种子数据 | 空数据库可一键建表、校验并可重建 | `[x]` |
| 4 | 身份与权限 | 实现平台身份解析、员工状态、角色和楼宇数据范围控制 | 身份伪造、越权和跨楼宇访问被拒绝 | `[x]` |
| 5 | 流程基础设施 | 实现采购状态机、事务、乐观锁、幂等和统一错误码 | 非法流转、并发覆盖和重复操作被阻止 | `[x]` |
| 6 | 采购主流程 | 实现需求人、楼长、采购员和仓库管理员完整流程 | 自动化测试跑通创建、驳回、重提、采购和入库 | `[x]` |
| 7 | 供应商与推荐 | 实现供应商主档、资料快照、黑名单和历史推荐 | 黑名单过滤及显式主档更新规则正确 | `[x]` |
| 8 | Agent 后端支撑 | 为 Agent 提供会话、消息、状态存储接口、Redis 缓存和快照恢复 | Agent 可通过接口读写数据，Redis 丢失后可从 MySQL 恢复关键状态 | `[x]` |
| 9 | 通知系统 | 实现 notification_outbox、异步发送、重试和补发 | 通知失败不回滚业务，失败记录可重试 | `[x]` |
| 10 | 测试与联调 | 完成单元、集成、接口、权限、安全和端到端测试 | 核心测试通过，OpenAPI 可供 Agent 联调 | `[x]` |
| 11 | 交付与发布 | 完成部署、迁移、回滚文档，提交并推送 GitHub | main 或约定分支具备可部署版本 | `[-]` |

## 已冻结业务规则

- 一张采购申请只包含一种设备。
- 采购状态统一为 `DRAFT`、`PENDING_REVIEW`、`REJECTED`、`PENDING_PURCHASE`、`PURCHASING`、`PENDING_WAREHOUSE`、`COMPLETED`。
- 预计价格和实际价格均保存单价与总价，总价由后端计算并校验。
- 每个楼长审核轮次对应一条 `purchase_review`，从 `DRAFT` 更新为 `COMPLETED`。
- 入库数量必须大于 0，可以少于、等于或多于申请数量；少于申请数量时备注必填。
- 本次采购始终保存供应商资料快照；只有采购员明确确认时才更新供应商主档。
- 有效黑名单可由原登记楼长或系统管理员提前解除。
- 关键操作使用 `expected_version` 防止并发覆盖，使用 `action_token` 防止重复执行。
- 平台消息使用 `external_message_id` 去重。
- 正式业务事实保存在 MySQL，Redis 只保存 Agent 实时短期状态。
- 通知通过 Outbox 异步发送，发送失败不回滚采购事务。

## 已完成任务：1. 技术基线

技术选型文档已经确定：

- Python 3.11+；本项目实际使用 Python 3.12.13。
- FastAPI 模块化单体，不使用 Spring Boot 或微服务。
- Router、Schema、Service、Domain、Repository、Integration 分层。
- SQLAlchemy 2.x 和 Alembic。
- MySQL 8.x 保存业务事实、历史记录、完整消息和关键会话快照。
- Redis 及 redis-py asyncio 保存实时会话、缓存、短期去重和临时锁。
- Pydantic 负责请求、响应、配置和结构化输出。
- httpx 负责异步外部 HTTP 调用。
- pytest 负责单元、接口和集成测试。
- Docker Compose 负责可复现的服务编排。
- OpenAPI 作为 Agent 和其他调用方的接口契约。
- Agent 不直接修改 ORM 模型，只能调用业务 Service 或 HTTP API。

项目补充采用：

- `pyproject.toml` 统一维护项目元数据、依赖和工具配置。
- pydantic-settings 管理环境配置。
- pytest-asyncio 支持异步测试。
- Ruff 负责代码检查和格式化。

开发环境约定：

- 开发阶段使用 Docker Compose 运行 MySQL 8.x。
- MySQL 数据使用独立 Docker Volume 持久化，不与本机现有 MySQL 数据目录混用。
- Redis 同样通过 Docker Compose 启动。
- FastAPI 开发服务先在本机 Conda 环境运行；部署阶段再将 FastAPI 一并容器化。
- 项目根目录 `.env` 使用 Docker MySQL 和 Redis 的开发连接配置，真实密钥不提交 Git。

Docker 隔离硬性要求：

- Compose 项目名固定为 `procurement-agent`。
- MySQL 容器名固定为 `procurement-agent-mysql`，主机端口使用 `3307`。
- Redis 容器名固定为 `procurement-agent-redis`，主机端口使用 `6380`。
- 独立网络固定为 `procurement-agent-network`，不得加入其他项目网络。
- 独立数据卷固定为 `procurement-agent-mysql-data` 和 `procurement-agent-redis-data`。
- MySQL 独立数据库固定为 `procurement_agent`。
- MySQL 使用独立应用账号 `procurement_agent_app`，应用不使用 root 账号。
- Redis 使用独立容器实例和独立数据卷，不连接或复用其他项目 Redis。
- Compose 不声明 external network 或 external volume，不复用现有 Docker 资源。
- 不挂载本机已有 MySQL 数据目录，不修改本机 MySQL 配置和数据。
- 未经明确确认不得执行 `docker compose down -v` 或删除项目数据卷。

验收条件：

- `[x]` 技术组合得到确认。
- `[x]` 明确使用 SQLAlchemy AsyncSession 和 asyncmy。
- `[x]` 明确开发环境中 MySQL 和 Redis 通过 Docker Compose 运行。
- `[x]` 明确首期提供 HTTP REST API。
- `[x]` 技术决策写入技术选型文档 V1.1。

## 已完成任务：2. 工程初始化

计划工作：

- 创建 `pyproject.toml` 并固定首批依赖。
- 创建 FastAPI 模块化单体目录。
- 建立异步 SQLAlchemy 引擎和 AsyncSession 工厂。
- 建立 pydantic-settings 配置体系和 `.env.example`。
- 创建 Docker Compose：MySQL 主机端口 3307、Redis 主机端口 6380、独立数据卷。
- Docker Compose 使用项目独立容器、网络、数据卷、数据库、数据库账号和 Redis 实例。
- 建立统一响应、异常处理、请求 ID 和结构化日志。
- 实现 `/health` 与 `/ready`。
- 初始化 Alembic 异步迁移环境。
- 建立 pytest、pytest-asyncio 和 Ruff 配置。

验收条件：

- `[x]` Docker MySQL 和 Redis 已启动并通过健康检查。
- `[x]` 容器、网络和数据卷名称均带 `procurement-agent` 前缀，且未连接任何其他项目网络或数据卷。
- `[x]` Docker MySQL 只创建本项目数据库和独立应用账号，不影响本机 MySQL。
- `[x]` 应用连接本项目 Docker MySQL 和 Docker Redis，不连接现有服务。
- `[x]` FastAPI 可在 `purchasing-agent` Conda 环境启动。
- `[x]` `/health` 正常返回。
- `[x]` `/ready` 已成功检查 MySQL 和 Redis。
- `[x]` Alembic 可连接 Docker MySQL。
- `[x]` 基础测试和 Ruff 检查通过。

## 已完成任务：3. 新数据库建表

当前状态：

- Docker 独立数据库 `procurement_agent` 已完成首次建表。
- 已创建 17 张业务表和 1 张 Alembic 版本表。
- 已初始化 5 个角色和一号楼至六号楼，未创建员工或管理员账号。
- 已通过升级、降级、重新升级和重复升级验证。
- 数据库结构与 SQLAlchemy 模型一致，无待生成的结构变更。

计划工作：

- 根据数据库设计 V1.3 建立 SQLAlchemy 模型。
- 使用第一版 Alembic 建表脚本，在空数据库中创建基础数据、采购业务、Agent 和通知 Outbox 表。
- 创建外键、唯一约束、组合索引、乐观锁字段和幂等约束。
- 固化金额、数量、税率、日期、状态和快照字段类型。
- 创建角色种子数据和可重复执行的初始化逻辑。
- 验证空数据库升级、降级、重新升级。
- 验证数据库中只存在本项目表和 Alembic 版本表。

验收条件：

- 所有设计表、字段、索引和约束与数据库设计 V1.3 一致。
- `alembic upgrade head` 可从空数据库成功执行。
- `alembic downgrade base` 可完整回滚本期结构。
- 再次升级后结构一致。
- 初始角色数据无重复，重复执行安全。
- 模型、迁移及数据库结构测试通过。

## 已完成任务：4. 身份与权限

计划工作：

- 明确外部平台身份与本项目员工账号的绑定规则。
- 实现员工、角色、楼宇管理范围的数据访问层。
- 实现统一的当前用户解析和权限校验。
- 拒绝停用员工、身份伪造、角色越权和跨楼宇访问。
- 补充身份与权限单元测试及数据库集成测试。

验收条件：

- 外部身份只能映射到有效员工。
- 不同角色只能执行其被授权的操作。
- 楼长只能访问其负责楼宇的数据。
- 停用员工、伪造身份和越权访问均返回明确错误。
- 身份与权限自动化测试通过。

完成结果：

- 采用可信飞书适配层/内部网关方案，并使用 HMAC-SHA256 保护身份上下文。
- 使用时间戳和 Redis 随机数防止过期签名及身份请求重放。
- 通过平台类型和平台用户标识解析本地员工、有效角色和有效楼宇。
- 实现 `/api/v1/users/me` 和合法处理人候选接口。
- 实现停用员工、伪造签名、角色越权和跨楼宇访问拦截。
- 默认对接口返回的手机号进行脱敏。

## 已完成任务：5. 流程基础设施

计划工作：

- 建立采购状态机和各操作允许的状态转换表。
- 实现事务边界、乐观锁版本校验和幂等操作令牌。
- 统一流程错误码和状态冲突响应。
- 建立操作日志写入规则和事务内审计能力。
- 补充非法状态流转、并发覆盖及重复操作测试。

验收条件：

- 未定义的状态转换被拒绝。
- 过期版本号不能覆盖最新业务数据。
- 同一个操作令牌不会重复执行业务。
- 状态更新和操作日志在同一事务内成功或回滚。
- 流程基础设施自动化测试通过。

完成结果：

- 建立 7 个采购状态和 7 个主流程操作的唯一转换表。
- 每个转换同时校验操作角色、当前处理人和楼宇范围。
- 使用 `expected_version` 和原子条件更新防止并发覆盖。
- 使用全局唯一 `action_token` 拒绝重复操作。
- 状态更新和操作日志由同一数据库事务提交或回滚。
- 下一处理人再次按有效员工、角色、平台身份和楼宇范围校验。

## 已完成任务：6. 采购主流程

计划工作：

- 实现需求人创建草稿、修改字段和提交楼长审核。
- 实现楼长保存审核字段、驳回、重新提交和审核通过。
- 实现采购员开始采购、保存采购执行信息并提交仓管。
- 实现仓库管理员保存入库信息并完成流程。
- 实现采购详情、列表和允许操作查询。
- 完成主流程端到端自动化测试。

验收条件：

- 创建、提交、驳回、修改、重提、审核通过、采购和入库完整跑通。
- 每个接口只允许当前角色和当前处理人操作。
- 金额、合同、供应商和入库数量条件得到正确校验。
- 关键操作均生成不可重复的日志记录。
- 主流程端到端自动化测试通过。

完成结果：

- 实现采购申请创建、字段保存、详情和个人相关列表接口。
- 实现提交审核、驳回、重提、保存审核方案和提交采购员。
- 实现开始采购、保存采购快照、提交仓管、保存入库和完成流程。
- 完成时在同一事务内生成需求人、楼长和采购员通知 Outbox 记录。
- 银行账号按角色脱敏，供应商资料始终保存本次采购快照。
- 跑通包含一次驳回、重提、合同、采购和少量入库说明的完整端到端流程。

## 已完成任务：7. 供应商与推荐

计划工作：

- 实现供应商搜索、详情、新增和资料补充接口。
- 实现采购快照与供应商主档显式同步规则。
- 实现永久、限时和提前解除的供应商黑名单。
- 实现品牌型号、历史采购和供应商推荐查询。
- 排除测试、无效及有效黑名单供应商，并显示历史风险。
- 补充供应商和推荐自动化测试。

验收条件：

- 未明确确认时，采购快照不会修改供应商主档。
- 明确确认同步时，供应商主档按本次资料更新。
- 有效黑名单供应商不会被静默推荐。
- 原登记楼长或管理员可以提前解除黑名单。
- 推荐结果只读且包含来源时间和风险信息。
- 供应商与推荐自动化测试通过。

完成结果：

- 实现供应商名称或统一社会信用代码搜索、详情查询和采购员/管理员新增供应商。
- 供应商银行账号按角色脱敏，采购员和管理员可查看完整财务资料。
- 本次采购始终保存供应商资料快照；仅在采购员明确确认时同步更新供应商主档。
- 实现楼长登记永久或限时黑名单，以及原登记楼长或管理员提前解除黑名单。
- 实现品牌型号、相似采购历史和历史供应商推荐，并排除测试数据和有效黑名单供应商。
- 历史采购仍保留被列入黑名单供应商的记录，并显式返回当前风险状态。

## 已完成任务：8. Agent 后端支撑

计划工作：

- 为外部 Agent 提供活动会话获取或创建、状态查询、状态更新、快照和完成接口。
- 为外部 Agent 提供用户、Agent 和系统消息的写入与查询接口，并实现平台消息幂等去重。
- 为外部 Agent 提供会话上下文和结构化状态的保存、读取接口。
- 使用 Redis 保存实时短期状态，在 MySQL 保存消息事实和关键状态快照。
- 实现 Redis 状态丢失后的 MySQL 快照恢复。
- 补充接口权限、状态流转、重复消息和缓存恢复自动化测试。

验收条件：

- 外部 Agent 可以通过稳定接口读写会话、消息和结构化状态。
- 用户只能访问自己的会话，管理员按约定范围查询。
- 相同平台消息不会重复写入。
- 会话状态变更、消息和关键快照可靠持久化。
- Redis 数据清空后可以从 MySQL 恢复关键会话状态。
- Agent 后端支撑接口自动化测试通过。

不在本项目后端范围内：

- 大模型调用、模型选择和推理流程。
- 提示词、意图识别和自然语言回复生成。
- Agent 工具选择、任务规划和业务决策编排。
- Agent 前端或外部平台机器人本体。

完成结果：

- 实现同一员工、同一业务动作复用一个活动会话，不同动作可保留独立会话。
- 实现用户、Agent、系统消息写入和分页读取，平台消息标识在会话内幂等。
- 实现结构化短期状态读写，Redis Key 为 `agent:session:{conversation_id}`，默认 TTL 为 72 小时。
- 实现关键状态写入 `agent_session_state`，Redis 状态丢失时自动从 MySQL 快照恢复。
- 实现会话完成时保存最终快照、关联采购申请并清理 Redis 状态。
- 会话只允许所属员工访问，并校验所关联采购申请的数据权限。
- 接口文档已同步为 V1.4，明确采购后端与外部 Agent 的职责边界。

## 已完成任务：9. 通知系统

计划工作：

- 实现 `notification_outbox` 待发送记录查询和领取。
- 实现异步发送适配接口，不在采购业务事务中直接调用外部平台。
- 实现失败重试、重试次数、下次重试时间和失败原因记录。
- 实现管理员或后台任务的失败通知补发能力。
- 补充发送成功、发送失败、重复领取、重试和补发自动化测试。

验收条件：

- 通知发送失败不会回滚已完成的采购业务。
- 同一通知不会被多个任务重复发送。
- 失败记录按照重试策略再次发送，并保留失败原因。
- 达到重试上限的通知可以被明确查询和补发。
- 通知系统自动化测试通过。

完成结果：

- 实现后台任务按 `PENDING` 或到达重试时间的 `FAILED` 记录领取通知。
- 使用 `SELECT FOR UPDATE SKIP LOCKED` 防止多个任务重复领取同一通知。
- 实现平台无关 HTTP 发送网关适配器，使用 `dedup_key` 作为发送幂等键。
- 实现发送成功落账、失败原因记录、指数退避和最大重试次数控制。
- 实现管理员通知查询、手动触发到期发送和失败通知人工补发接口。
- 人工补发复用原通知记录并写入采购操作日志，不生成重复业务通知。
- 未配置实际通知网关时按发送失败处理，不会误标记为发送成功。

## 已完成任务：10. 测试与联调

计划工作：

- 汇总和检查全部 OpenAPI 接口、请求模型、响应模型与错误码。
- 补充缺失的权限边界、非法输入和异常恢复测试。
- 启动本地 FastAPI 服务，对 MySQL、Redis 和主要接口执行真实 HTTP 联调。
- 准备供外部 Agent 和通知网关使用的接口说明、环境变量清单和联调示例。
- 检查日志中是否泄露数据库密码、网关令牌或未脱敏敏感字段。

验收条件：

- 全量自动化测试、Ruff 和 Alembic 模型偏差检查通过。
- `/health`、`/ready` 和全部核心业务接口可通过真实 HTTP 请求访问。
- OpenAPI 中不存在未注册、重复或与文档冲突的核心接口。
- 外部 Agent 可以依据接口说明完成联调，不需要直接访问 MySQL 或 Redis。
- 密码、签名密钥和通知网关令牌不出现在代码、文档或日志中。

完成结果：

- 补齐接口文档中遗漏实现的采购历史查询和采购流程时间线接口。
- 建立 OpenAPI 契约测试，持续检查核心路径、操作标识、统一响应和身份依赖。
- 文档中的 32 个业务路径与实际 OpenAPI 的 32 个业务路径完全一致。
- 全项目 43 项自动化测试、Ruff 和 Alembic 模型偏差检查通过。
- 临时启动本地 FastAPI，完成健康、MySQL、Redis、签名身份、采购历史、时间线、
  供应商、Agent 会话和管理员通知共 10 项真实 HTTP 联调。
- 验证未签名业务请求被拒绝，未发现硬编码数据库密码、签名密钥或通知网关令牌。
- 完成外部 Agent 和通知网关联调说明。

## 当前任务：11. 交付与发布

计划工作：

- 检查生产配置项、Docker 部署方式和数据库升级步骤。
- 编写部署、升级、备份和回滚说明。
- 初始化本地 Git 仓库并设置仓库级 `schannel`。
- 检查提交范围，确保 `.env`、`.env.docker` 和运行产物不进入 Git。
- 提交代码并推送到 `fengmitang/Procurement-Agent` 的 `main` 分支。

验收条件：

- 新环境可以按照文档启动 MySQL、Redis 和 FastAPI。
- 数据库升级和应用回滚步骤明确且可执行。
- Git 提交不包含密码、令牌、缓存、日志或临时产物。
- GitHub `main` 分支包含当前可部署版本。

## 进度维护规则

每完成一个阶段：

1. 执行该阶段对应的测试或检查。
2. 对照完成标准确认是否真正完成。
3. 将状态从 `[-]` 更新为 `[x]`。
4. 将下一阶段改为 `[-]`。
5. 记录完成日期、验证结果及遗留问题。
6. 向用户汇报本阶段结果，并只给出紧接着的下一步。

## 进度记录

### 2026-07-30

- `[x]` 明确本项目只建设供外部 Agent 调用的后端接口与存储，不实现 Agent 推理和编排。
- `[x]` 完成活动会话获取或创建、消息写入与分页查询接口。
- `[x]` 完成结构化状态读写、关键快照保存和会话完成接口。
- `[x]` 完成平台消息幂等、会话归属权限和采购申请关联权限校验。
- `[x]` 验证 Redis 状态清空后可从 MySQL 快照自动恢复。
- `[x]` Agent 后端支撑专项 2 项、全项目 22 项自动化测试全部通过。
- `[x]` Ruff 代码检查和 Alembic 模型结构偏差检查通过。
- `[x]` 生成后端接口文档 V1.4，并通过内容与接口结构校验。
- `[!]` 当前环境缺少 LibreOffice，接口文档 V1.4 未执行页面渲染视觉检查。
- `[x]` 完成通知 Outbox 到期领取、异步发送适配、成功和失败状态落账。
- `[x]` 完成指数退避、最大重试次数和达到上限后停止自动重试。
- `[x]` 完成管理员通知查询、手动触发发送和失败通知人工补发。
- `[x]` 完成人工补发操作日志和 action_token 幂等控制。
- `[x]` 通知系统专项 3 项、全项目 25 项自动化测试全部通过。
- `[x]` Ruff 代码检查和 Alembic 模型结构偏差检查通过。
- `[x]` 生成后端接口文档 V1.5，并通过内容与接口结构校验。
- `[!]` 当前环境缺少 LibreOffice，接口文档 V1.5 未执行页面渲染视觉检查。
- `[x]` 补齐采购历史查询和采购流程时间线接口。
- `[x]` 建立 OpenAPI 契约测试，文档和实际代码均为 32 个业务路径且完全一致。
- `[x]` 全项目 29 项自动化测试、Ruff 和 Alembic 模型结构检查通过。
- `[x]` 完成 10 项真实 HTTP 联调，并在结束后停止临时服务、清理临时会话。
- `[x]` 验证业务接口统一要求签名身份，未发现硬编码敏感配置。
- `[x]` 完成外部 Agent 和通知网关联调说明。
- `[x]` 增加仅限开发环境的本地功能体验界面，覆盖采购、供应商、历史、Agent 存储和通知。
- `[x]` 体验界面通过后端内部签名代理调用真实业务接口，浏览器不保存网关密钥。
- `[x]` 完成界面入口、身份权限和路径限制测试。
- `[x]` 使用真实浏览器验证角色切换、采购详情、供应商、历史、Agent 和通知页面，无控制台错误。
- `[x]` 将采购体验页升级为需求人、楼长、采购员和仓管四类角色业务工作台。
- `[x]` 补齐楼长修改员工需求字段和查询管辖楼宇全部申请的后端能力。
- `[x]` 真实浏览器从新建采购单走到完成，验证草稿、提交校验、审核、采购、少量入库及角色历史记录。
- `[x]` 修复体验数据与固定测试数据之间的重置隔离；全项目 34 项自动化测试通过。
- `[x]` 将“设备专业”统一展示为“设备类型”，前后端限定为 8 个已确认选项。
- `[x]` 增加设备类型全部合法值及非法值校验；全项目 43 项自动化测试通过。
- `[x]` 申请详情和角色历史记录补齐提交、审批、采购、入库及完成时间。
- `[x]` 申请流程履历按步骤保留经办人、角色、联系方式和操作时间。
- `[x]` 联系方式默认脱敏，并增加有权用户点击后按单条流程记录查看完整手机号的接口。
- `[x]` 未保存修改时阻止流程提交，并按角色提示用户先保存；常见接口错误改为中文操作提示。
- `[x]` 本轮全项目 43 项自动化测试和 Ruff 检查通过。
- `[x]` 修复浏览器提交采购时间时被按 UTC 时钟值落库造成的 8 小时时差。
- `[x]` 需求人申请详情移除“缺失字段”和采购执行快照，继续保留流程履历。
- `[x]` 验证楼长、采购员和仓管所需详情内容不受需求人页面精简影响。
- `[x]` 编写从全新克隆到本地启动的环境搭建指南，覆盖依赖、配置、迁移、验证和排错。
- `[x]` 按当前 OpenAPI 重写跨模块后端联调说明，完整覆盖 37 个业务接口操作。
- `[x]` 清理 Git 文档目录，各版本化文档系列仅保留当前最新版本。
- `[-]` 开始部署、升级、回滚、Git 初始化和 GitHub 发布准备。

### 2026-07-29

- `[x]` 完成三份文档一致性修订。
- `[x]` 生成需求 V0.6、数据库 V1.3、接口 V1.1。
- `[x]` 完成 DOCX 结构、字段和章节校验。
- `[x]` 读取技术选型文档 V1.0，并将已确定方案写入任务清单。
- `[x]` 确认当前环境为 Python 3.12.13、MySQL 8.0 和 SQLAlchemy 2.x。
- `[x]` 确认开发阶段使用 Docker MySQL 和 Docker Redis。
- `[x]` 确认使用 SQLAlchemy AsyncSession 和 asyncmy。
- `[x]` 生成并校验技术选型文档 V1.1。
- `[x]` 技术基线阶段通过验收。
- `[x]` Docker Desktop 已启动，Docker Server 29.2.1 可用。
- `[x]` 已检查现有 Docker 资源和端口；主机端口 3307、6380 当前空闲。
- `[x]` 已冻结 Docker 完全隔离要求，不复用现有容器、网络、数据卷、数据库或 Redis。
- `[x]` 完成 FastAPI、异步 SQLAlchemy、Alembic、配置、日志、异常处理和健康检查骨架。
- `[x]` 完成 Docker MySQL/Redis 启动和完全隔离验收。
- `[x]` 完成 `/health`、`/ready`、Alembic 连接、pytest 和 Ruff 验证。
- `[x]` 工程初始化阶段通过验收。
- `[x]` 完成 17 张业务表的 SQLAlchemy 模型和首版 Alembic 建表脚本。
- `[x]` 初始化 5 个角色和一号楼至六号楼，员工数据保持为空。
- `[x]` 完成升级、降级、重新升级、重复升级和模型结构偏差检查。
- `[x]` 数据库结构测试、现有测试和 Ruff 检查全部通过。
- `[x]` 创建可重复执行和单独清理的全流程 `TEST` 虚拟数据脚本。
- `[x]` 写入 9 个采购场景，覆盖全部采购状态和主要业务分支。
- `[x]` 覆盖身份权限、供应商快照、黑名单、Agent 会话和通知状态测试数据。
- `[x]` 连续两次执行虚拟数据脚本，并通过数据数量及场景完整性校验。
- `[x]` 确认由飞书适配层或内部网关验证身份，采购后端只接受网关签名身份。
- `[x]` 完成当前用户解析、角色和楼宇权限、候选处理人查询及手机号脱敏。
- `[x]` 完成伪造签名、重放、未知用户、停用员工、角色越权和跨楼宇测试。
- `[x]` 身份与权限阶段共 9 项自动化测试通过，数据库模型无结构偏差。
- `[x]` 生成接口文档 V1.2 和技术选型文档 V1.2，并通过 DOCX 结构校验。
- `[!]` 当前环境缺少 LibreOffice，两份 V1.2 文档未执行页面渲染视觉检查。
- `[x]` 建立采购状态机和角色、当前处理人、楼宇及下一处理人校验规则。
- `[x]` 完成事务、乐观锁、幂等令牌和事务内操作日志基础设施。
- `[x]` 非法状态、旧版本、重复操作、跨楼宇处理人和事务回滚测试通过。
- `[x]` 流程基础设施专项 6 项、全项目 15 项自动化测试全部通过。
- `[x]` 完成需求人、楼长、采购员和仓库管理员全部采购主流程接口。
- `[x]` 跑通创建、驳回、修改、重提、审核、采购、少量入库和完成的端到端流程。
- `[x]` 验证空草稿禁止提交、错误金额禁止保存、少量入库说明必填。
- `[x]` 修正草稿字段数据库约束，结构升级脚本通过回退和再升级验证。
- `[x]` 生成数据库设计 V1.4、接口文档 V1.3 和技术选型 V1.3，并通过 DOCX 结构校验。
- `[!]` 当前环境缺少 LibreOffice，三份递增版文档未执行页面渲染视觉检查。
- `[x]` 采购主流程完成后，全项目 16 项自动化测试及模型偏差检查通过。
- `[x]` 完成供应商主档搜索、详情脱敏和新增接口。
- `[x]` 完成采购资料快照与显式同步供应商主档规则。
- `[x]` 完成永久、限时、提前解除黑名单及登记/解除权限控制。
- `[x]` 完成品牌型号、相似采购历史和供应商推荐接口。
- `[x]` 验证测试数据不参与普通推荐，有效黑名单供应商不参与供应商推荐。
- `[x]` 供应商与推荐专项 4 项、全项目 20 项自动化测试全部通过。
- `[x]` Ruff 代码检查和 Alembic 模型结构偏差检查通过。
