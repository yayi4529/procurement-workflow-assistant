# 采购助手交接（2026-08-10）

## 服务状态

- 后端：`http://127.0.0.1:8001`
- Agent：`http://127.0.0.1:8003`
- 飞书回调：当前 Quick Tunnel 地址请从 `.local/cloudflared-8003.log` 获取，并在飞书开放平台追加 `/webhooks/feishu`。
- 8003 已启用飞书、LLM 和通知网关；后端通知 Worker 已启动并轮询投递。
- 不读取、输出或提交 `.env` 密钥。

## 已完成

- 29 条测试采购单已写入后端并完成全流程：`91046`～`91074`。
- 采购单 `91077`：楼长已提交给采购员；采购员通知 `99912` 已发送。
- 后端新增单条通知调度接口：`POST /api/v1/notifications/{notification_id}/dispatch`（仅 ADMIN，且仅 PENDING）。
- Agent 通知网关已修复并由后端配置调用。
- 采购员卡片已移除“采购时间”手填项；保存时 Agent 自动使用系统时间。
- LLM 思考模式对 `tool_choice` 的兼容回退已加入。
- 测试：`tests/unit/test_assistant_foundation.py`，8 passed（pytest cache 权限警告不影响结果）。

## 当前身份

- 需求人 `ou_3da4d0e3765ebe815eae094a70e0ca8d` 已额外授予 `ADMIN`（员工 90001），用于通知管理。
- 楼长、采购员、仓库管理员的绑定沿用此前配置；不要在日志中输出完整 open_id。

## 下一步

1. 在飞书确认采购员已收到 `91077` 的卡片；刷新会话即可。
2. 采购员完成并提交仓库后，检查 `91077` 是否生成 `REQUIREMENT_PENDING_WAREHOUSE`；Worker 应自动推送仓库卡片。
3. 若卡片未推送：
   - 查 `.local/agent-8003-notification.log`；
   - 管理员调用 `GET /api/v1/notifications?request_id=91077`；
   - 对 PENDING 的单条通知调用新 dispatch 接口，不要全局批量调度旧导入数据。

## Git

- Agent 分支：`chore/feishu-fake-debug-harness`。
- Agent 源码存在未提交改动；后端新增接口也未提交。
- 未 commit、未 push、未创建 PR。
